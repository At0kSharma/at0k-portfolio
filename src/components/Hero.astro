---
import { PROFILE } from "../config/content";
---
<header class="masthead" id="top">
  <h1 class="name">{PROFILE.name}<span>.</span></h1>
  <p class="thesis">{PROFILE.thesis}</p>
  <div class="whereabouts">
    {PROFILE.whereabouts.map((item) => <span>{item}</span>)}
  </div>
</header>
